"use strict";

function ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); if (enumerableOnly) symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; }); keys.push.apply(keys, symbols); } return keys; }

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; if (i % 2) { ownKeys(Object(source), true).forEach(function (key) { _defineProperty(target, key, source[key]); }); } else if (Object.getOwnPropertyDescriptors) { Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)); } else { ownKeys(Object(source)).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } } return target; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

var burgerMenu = function burgerMenu() {
  var e = document.querySelector(".headerBurger"),
      t = document.querySelector(".headerMenu"),
      n = document.querySelector(".body");
  e && e.addEventListener("click", function () {
    e.classList.toggle("active"), t.classList.toggle("active"), n.classList.toggle("blocked");
  });
};

burgerMenu();

var langMenu = function langMenu() {
  var e = document.querySelector(".headerMenu"),
      t = document.querySelector(".langMenu"),
      n = document.querySelector(".langContainer");
  t && (window.matchMedia("(max-width: 768px)").matches && !t.classList.contains("active") ? (t.classList.add("active"), e.prepend(t)) : (t.classList.remove("active"), n.prepend(t)));
};

langMenu();

var headerSubMenu = function headerSubMenu() {
  var e = document.querySelector(".subMenu"),
      t = document.querySelector(".headerSubMenu");
  e && e.addEventListener("click", function () {
    t.classList.toggle("active");
  });
};

headerSubMenu();
var slideIndex = 1;

var slides = document.querySelectorAll(".slidesItem"),
    showSlides = function showSlides(e, t) {
  if (0 == t.length) return !1;
  e > slides.length ? slideIndex = 1 : e < 1 && (slideIndex = t.length);

  for (var _e = 0; _e < t.length; _e++) {
    t[_e].classList.remove("active");
  }

  t[slideIndex - 1].classList.toggle("active");
};

showSlides(slideIndex, slides);
var prevBtn = document.querySelector(".prevBtn");
prevBtn && prevBtn.addEventListener("click", function (e) {
  e.preventDefault(), showSlides(slideIndex -= 1, slides);
});
var nextBtn = document.querySelector(".nextBtn");
nextBtn && nextBtn.addEventListener("click", function (e) {
  e.preventDefault(), showSlides(slideIndex += 1, slides);
});

var popUp = document.querySelector(".popUp"),
    popUpSpinner = document.querySelector(".popUpSpinner"),
    popUpMsg = document.querySelector(".popUpMsg"),
    formSend = function formSend(e) {
  popUp.classList.add("active"), popUpSpinner.classList.add("active"), fetch("includes/mail.php", {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      Accept: "application/json",
      "Accept-Charset": "utf-8"
    },
    body: JSON.stringify(e)
  }).then(function (e) {
    console.log(e);
  }).then(function () {
    popUpSpinner.classList.remove("active"), popUpMsg.classList.add("active"), setTimeout(function () {
      popUp.classList.remove("active");
    }, 4e3);
  })["catch"](function (e) {
    return console.log(e);
  });
},
    formValidation = function formValidation(e, t, n) {
  var o = document.querySelector(".errMsg"),
      a = {
    name_email: "Enter the valid name and email address",
    phone_email: "Enter the valid phone and email address",
    name_phone: "Enter the valid name and phone",
    name: "Enter the valid name",
    email: "Enter the valid email address",
    message: "Fill the required fields"
  };
  var l = null;
  if (e.value || t.value || n.value ? t.value || n.value ? e.value || n.value ? e.value || t.value ? e.value ? t.value ? n.value || (l = "email") : l = "phone" : l = "name" : l = "name_phone" : l = "name_email" : l = "phone_email" : l = "message", l) return o.innerHTML = a[l], o.classList.add("active"), !1;
  l || o.classList.remove("active"), console.log(l);
},
    formData = function formData() {
  var e = document.getElementsByTagName("form"),
      t = document.querySelector(".inputName"),
      n = document.querySelector(".inputPhone"),
      o = document.querySelector(".inputEmail");

  for (var a = 0; a < e.length; a++) {
    e[a].addEventListener("submit", function (e) {
      e.preventDefault(), formValidation(t, n, o);
      var a = new FormData(this);
      a = Object.fromEntries(a), formSend(a), this.reset();
    });
  }
};

formData();

var setLanguage = function setLanguage() {
  document.querySelectorAll(".headerLang").forEach(function (e) {
    e.addEventListener("click", function () {
      var t = window.location.href.split("?")[0],
          n = e.getAttribute("data-lang");
      return setCookie("lang", n, {
        "max-age": 3600
      }), location.href = t + "?lang=" + n, !1;
    });
  });
};

document.querySelectorAll(".headerLang").forEach(function (e) {
  e.addEventListener("click", function () {
    var t = window.location.href.split("?")[0],
        n = e.getAttribute("data-lang");
    return setCookie("lang", n, {
      "max-age": 3600
    }), location.href = t + "?lang=" + n, !1;
  });
});

var getCookie = function getCookie(e) {
  var t = document.cookie.match(new RegExp("(?:^|; )" + e.replace(/([\.$?*|{}\(\)\[\]\\\/\+^])/g, "\\$1") + "=([^;]*)"));
  return t ? decodeURIComponent(t[1]) : void 0;
},
    setCookie = function setCookie(e, t) {
  var n = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
  (n = _objectSpread({
    path: "/"
  }, n)).expires instanceof Date && (n.expires = n.expires.toUTCString());
  var o = encodeURIComponent(e) + "=" + encodeURIComponent(t);

  for (var _e2 in n) {
    o += "; " + _e2;
    var _t = n[_e2];
    !0 !== _t && (o += "=" + _t);
  }

  document.cookie = o;
},
    deleteCookie = function deleteCookie(e) {
  setCookie(e, "", {
    "max-age": -1
  });
};